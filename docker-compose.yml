services:
  mini-chat-figma:
    build: .
    container_name: mini-chat-figma
    env_file: .env
    ports:
      - "8787:8787"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8787/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
